<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Anchored Summary

### Goal
Complete Felexia ERP: onboarding company search, commercial documents, dashboard, branding, welcome checklist, reference data, Stripe subscriptions, user management, SaaS tunnel, demo data seeding, TVA export anomalies, supplier receipts, TVA declarations, RH module, runtime fixes.

### Done (most recent first)
- **Header landing : chevauchement « Demander une démo » sur la nav corrigé — grille 3 colonnes + breakpoint xl + découverte `2xl` non généré** — après diagnostic de la cause racine (nav non contrainte + démo en `2xl:inline-flex` **jamais émise** par Tailwind v4/Turbopack : le variant `2xl` n'existe pas dans ce build — vérifié empiriquement sur le CSS compilé, seul `@media (min-width:80rem)` (xl) + les variantes `max-*` sont générées ; `96rem` n'apparaît que dans la règle `.container`, zéro utilitaire `.2xl\:*` sur les 3 fichiers CSS du build) :
  - **Structure** : `LandingHeader.tsx` réécrit — `<header class="sticky top-0 z-50 border-b border-slate-200/70 bg-white/95 backdrop-blur-xl">` + `<div class="mx-auto grid h-20 max-w-[1440px] grid-cols-[auto_minmax(0,1fr)_auto] items-center gap-8 px-6 xl:px-8">` ; col1 logo `BrandLogo horizontal sm` (165×55 — **sans cap `height`**, le cap 44px clipait à 132px, cf. décision BrandLogo) ; col2 nav `hidden min-w-0 items-center justify-center gap-6 xl:flex` (liens `whitespace-nowrap text-sm font-medium text-slate-600 hover:text-[#0B2A5B]`, 6 liens conservés) ; col3 actions `hidden shrink-0 items-center justify-end gap-2 xl:flex` (démo `max-2xl:hidden h-11 px-4` en `landingSecondary` → visible ≥1536px via la base `inline-flex` de la variante ; Connexion ghost `h-11 px-3` ; Démarrer `landingPrimary h-11 px-5`) ; burger **`col-start-3`** + `xl:hidden` (sans `col-start-3`, le burger se plaçait en col2 juste après le logo quand nav/actions sont `hidden`) ; menu mobile conservé (6 liens + 3 actions).
  - **⚠ Motif display à retenir (ordre CSS compilé)** : base `.hidden{display:none}` @17346 **avant** `.inline-flex{display:inline-flex}` @17425 → `inline-flex` gagne si les 2 classes (base) coexistent sur un même élément. Motifs sûrs : `hidden … xl:flex` (variante media après la base), `max-2xl:hidden` (media block @87198, après la base `inline-flex` → caché <1536px, visible ≥1536px), ou `xl:inline-flex`. Ne JAMAIS associer `hidden` + `inline-flex` en base, ni compter sur `2xl:*` (non généré — ne plus utiliser de classes `2xl:` dans le projet).
  - **Vérifié** : lint 0 erreur, build 113 routes OK ; HTML prérendu : logo width=165/height=55, `grid-cols-[auto_minmax(0,1fr)_auto]`, `max-w-[1440px]`, `min-w-0`, `col-start-3`, zéro `absolute` dans le header, zéro `2xl:gap`/`2xl:px` ; CSS du build (`3z4o-97er_nc9.css`) : `max-2xl:hidden`@87198 après `inline-flex`@17425. Budget @1280 : logo 165 + nav ~600 (gap-6) + 2 actions ~360 + gaps 64 ≈ 1189 < 1232 ✓ ; @≥1536 : + démo 212 ≈ 1401 < 1376-1472 (marginal Geist 15-25 % plus étroit, à confirmer au déploiement). **PENDING**: deploy + vérif visuelle DevTools (largeurs 1920/1600/1440/1366/1280/1180/1024/768/390, zoom 90/100/125 %, aucun scroll horizontal, démo jamais sur la nav).
- **CTA landing : variante dédiée `landingPrimary` (fond uni `#1456B8`, blanc forcé `!important`, contenu z-10) — lisibilité garantie en cascade** — remplace la charte dégradé précédente (entrée ci-dessous) après diagnostic de la cascade CSS :
  - **Diagnostic** : CSS compilé OK (`.text-white{color:var(--color-white)}` présent, `--color-white` non surchargé) mais le blanc **non-`!important`** pouvait être écrasé par la couche de compat adaptative `!important` (`globals.css` l.281-352 : `.text-slate-* { color: ... !important }`) et le scope `.landing` `!important` (l.377-469) via `a { color: inherit }` (l.204) ; le dégradé `#0F3D91→#2F7CF6` chute aussi sous WCAG AA (4:1 < 4.5:1 côté clair) à 14px.
  - **Fix** : `src/components/ui/button.tsx` — composant original **restauré** (variants tokens `primary/secondary/ghost/danger` à l'identique, base `inline-flex h-10 ... rounded-[var(--radius-md)] px-4 ...`) + **2 nouvelles variantes** : `landingPrimary` (`border #174EA6`, `bg #1456B8`, `!text-white`, shadow `0_10px_24px rgba(20,86,184,.24)` / hover `0_14px_30px rgba(20,86,184,.30)`, hover `#0F478F` + `hover:!text-white`, ring focus `#4A90E2`) et `landingSecondary` (`border #DCE3EF`, `bg-white`, `!text-[#0F2548]`, hover `#F8FAFD` + `hover:!text-[#0F2548]`) ; base landing = `relative isolate inline-flex overflow-hidden rounded-2xl text-sm font-semibold ... disabled:pointer-events-none` **sans tailles** (éviter les conflits `cn` — `cn` = simple join sans tailwind-merge : h-12/px-7 du hero vs h-11/px-5 du header se fixent **par appel** en className) ; **`asChild` ajouté via `cloneElement`** (pas de `@radix-ui/react-slot` ni `class-variance-authority` dans le projet — aucune dépendance ajoutée).
  - **⚠ Incidents évités/documentés** : un premier `button.tsx` minimal écrasait le composant original (~200 pages utilisent `variant="primary|secondary|ghost|danger"`) → **récupéré depuis les chunks dev Turbopack** (`.next/dev/server/chunks/ssr/*` contiennent le module compilé : `"Button", ()=>Button`, variants tokens) → restauré à l'identique. À retenir : `src/components/ui/button.tsx` EXISTE (glob initial trompeur), et `npx eslint` hors projet installe eslint 10 incompatible — toujours `npm run lint`.
  - **Appliqué** : Header desktop + mobile (h-11 px-5 ; démo `2xl` en `landingSecondary`), Hero primaire+secondaire (h-12 px-7), Why, Comparison, Pricing (business `landingPrimary`, autres `landingSecondary`, h-12 px-7 full width), Contact submit (type submit, disabled).
  - **Vérifié** : lint 0, build 113 routes OK, CSS compilé contient `.\!text-white{color:var(--color-white)!important}` + `.\!text-\[\#0F2548\]{color:#0f2548!important}` + `#1456B8` ; HTML prérendu : `border-[#174EA6] bg-[#1456B8] !text-white shadow-[0_10px_24px_rgba(20,86,184,0.24)] hover:bg-[#0F478F] hover:!text-white` + `<span class="relative z-10 !text-white">` + href `/login?mode=register` ; `0F3D91` ne reste que sur le bandeau décoratif du tableau Comparison ; aucun `text-slate-*` sur les textes de CTA. **PENDING**: deploy + vérif visuelle DevTools (color rgb(255,255,255), hover, zoom 90/100/125).
- **Charte CTA premium (Stripe/Linear) — lisibilité des boutons principaux corrigée** — l'ancien dégradé navy `#061B3F→#1E66D0` était trop sombre (texte illisible à 100% zoom). Nouvelle charte appliquée à **tous les CTA principaux** (header « Démarrer gratuitement » + mobile, hero « Commencer l'essai Essentiel gratuit », Why, Comparison, Contact submit, Pricing business) : `bg-gradient-to-r from-[#0F3D91] to-[#2F7CF6]` (135°), `text-white`, `font-semibold`, `h-12` (48px), `px-7` (28px), `rounded-2xl` (16px), `shadow-[0_10px_30px_rgba(47,124,246,0.22)]`, hover `from-[#0C3278] to-[#2267D8]` + `hover:-translate-y-px` (1px), `transition duration-200 ease-out`, focus `focus:outline-2 focus:outline-[#2F7CF6]/35`. **Secondaire « Voir les tarifs »** : `border-[#DCE3EF] bg-white text-[#0F2548] hover:bg-[#F8FAFD]`. Zéro texte foncé sur fond bleu, zéro ancien navy restant (`#061B3F→#1E66D0` et `#082451` éliminés). **REMARQUE : REMPLACÉE par la variante `landingPrimary` (fond uni `#1456B8` + `!text-white`) ci-dessus** — le dégradé restait sous WCAG AA (4:1) et le blanc n'était pas forcé. Vérifié : lint 0 erreur, build 113 routes OK, HTML prérendu + live (11 occurrences du dégradé, shadow rgba(47,124,246,0.22), hover translate, focus ring, secondaire DCE3EF/0F2548), `/login?mode=register` 200. **PENDING**: deploy.
- **Landing : reprise complète « Executive Clarity » (chevauchements, CTA navy, hero équilibré, image agrandie)** — causes des erreurs visuelles : (1) **nav en `absolute left-1/2 -translate-x-1/2`** → chevauchait logo/actions aux largeurs 1280-1400px (logo 165 + 6 liens + 3 boutons ≈ 1330px > conteneur) ; (2) CTA `bg-[#0B63F6]` trop saturé ; (3) hero centré étroit + `<br>` en trop → titre coupé verticalement ; (4) image dans colonne 1.08fr sans extension → trop petite. Corrigé :
  - **`LandingHeader.tsx`** : **plus aucun `absolute`** — flex classique `justify-between` avec **3 enfants `flex-1`** (logo `justify-start` / nav / actions `justify-end`) → nav réellement centrée, zéro chevauchement possible. Breakpoints : nav `hidden xl:flex gap-6 xl:gap-7` (à xl : 165+572+334 = 1071px < 1216px dispo ✓) ; actions `hidden xl:flex gap-2.5` (Connexion + Démarrer) ; « Demander une démo » `hidden 2xl:inline-flex` (à 2xl : 1271px < 1472px ✓) ; hamburger `xl:hidden` (en dessous de xl tout passe dans le dropdown). Boutons : **primary navy** `h-11 rounded-2xl bg-gradient-to-r from-[#061B3F] to-[#1E66D0] px-6 text-white shadow-[0_14px_30px_rgba(30,102,208,0.22)] hover:from-[#082451] hover:to-[#155EEF]` ; démo outline `text-[#061B3F]` ; Connexion ghost `hover:text-[#061B3F]`.
  - **`LandingHero.tsx`** : grid `lg:grid-cols-[0.92fr_1.08fr] gap-12 xl:gap-16` (contenu sur toute la largeur max-w-7xl) ; titre en **2 lignes contrôlées** « Toute votre PME / dans un seul outil » (`<br className="hidden sm:block">` uniquement) `text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-bold leading-tight sm:leading-[1.05] lg:leading-[1.02]`, accent `from-[#1E66D0] to-[#38A3FF]` ; sous-titre `mt-6 max-w-xl text-lg leading-8` ; CTA `mt-8 flex flex-col gap-3 sm:flex-row` — primaire navy `h-12 px-7 shadow-[0_18px_40px_rgba(30,102,208,0.25)]`, secondaire border `text-[#061B3F]` ; trust `mt-7 gap-x-6 gap-y-3` checks `bg-blue-50` + `#1E66D0`. **Image agrandie** : wrapper `relative lg:-mr-8 xl:-mr-12` (déborde dans le padding, jamais hors viewport) + conteneur `rounded-[30px] border-slate-200/80 shadow-[0_30px_90px_rgba(15,23,42,0.14)] ring-1` + halo `-inset-6 rounded-[44px] bg-blue-500/10 blur-3xl`, `sizes="(max-width: 1024px) 100vw, 760px"` (largeur cible 680-780px desktop).
  - **`LandingProofStrip.tsx`** : section `relative -mt-6 pb-20` (bande remontée sous le hero, même max-w-7xl, px-6 lg:px-8), 5 items inchangés.
  - **CTA harmonisés navy partout** (Why, Comparison, Contact submit, Pricing ctaFor.business) : `from-[#061B3F] to-[#1E66D0]` + `hover:from-[#082451] hover:to-[#155EEF]` + `shadow-[0_18px_40px_rgba(30,102,208,0.25)]` (PowerShell replace, tuiles d'icônes `bg-gradient-to-br` conservées) ; toggles pricing + onglets features + dernière étape workflow passés en **solid `bg-[#1E66D0]`**. Zéro `#0B63F6` restant, zéro `hover:from-[#1A5BC2]`.
  - **Anti-scroll** : `globals.css` — `html, body { max-width: 100%; overflow-x: clip; }` ; wrapper `page.tsx` `min-h-screen overflow-x-clip bg-white text-slate-950`.
  - Vérifié : lint 0 erreur, build 113 routes OK, HTML prérendu + live (nav sans absolute, flex-1 ×2, CTA navy, grid 0.92/1.08, `-mr-12`, shadow rgba, badge « Recommandé » seul `-translate-x-1/2` restant — contenu dans sa card), `/login?mode=register` 200, aucun w-screen. **PENDING**: deploy.
- **Landing alignée sur la référence (header premium + hero 2 colonnes + proof strip + modules clés)** — retouche direction artistique (pas de nouveau style, version de l'image de référence) :
  - **`LandingHeader.tsx`** : nav **centrée** via `absolute left-1/2 -translate-x-1/2 xl:flex gap-8` (vraiment au milieu du conteneur, indépendante des largeurs latérales), nav affichée à xl (lg impossible : 6 liens + logo + 3 boutons > 1024px — breakpoint conservé et documenté), hover nav `#0B63F6`, logo **une seule instance `size="sm"`** (mobile + desktop, 165px), bouton primaire solid `bg-[#0B63F6] hover:bg-[#0858DC]`, démo outline `hover:text-[#0B63F6]`, mobile inchangé.
  - **`BrandLogo.tsx`** : tailles ajustées au spec — sm `w-[165px] max-h-[56px]` (attrs 165×55 ; cap = hauteur naturelle 55px pour que la largeur 165 soit réellement rendue, contrairement à l'ancien cap 44px qui clipait à 132px), md 200×66 max-h-54, lg 240×80 max-h-66, xl 260×86 max-h-74 ; mark sm 36 / md 44.
  - **`LandingHero.tsx`** : **2 colonnes** `grid items-center gap-14 lg:grid-cols-[0.85fr_1.15fr]` — gauche : badge pill bleu `bg-blue-50 text-[#0B63F6] ring-1 ring-blue-100`, h1 `text-4xl sm:text-5xl lg:text-6xl xl:text-7xl` navy `#061B3F` avec accent `bg-gradient-to-r from-[#0B63F6] to-[#38A3FF]`, sous-titre, CTAs (primaire gradient `from-[#0B63F6] to-[#1E66D0]` → `/login?mode=register`, secondaire border → `#pricing`), 3 trust points avec **check bleu** (`bg-blue-50` + icône `#0B63F6`) ; droite : screenshot réel `felexia-erp-dashboard-hero.png` dans conteneur premium (halo `-inset-6 rounded-[40px] bg-blue-500/10 blur-3xl`, `rounded-[28px] border-slate-200/80 shadow-2xl shadow-blue-950/10 ring-1`, `sizes="(max-width: 1024px) 100vw, 720px"`, `object-contain`, 1672×941). **Blobs + cubes décoratifs supprimés** (section 9 : ne garder que header/hero/screenshot/proof/modules). Fond `bg-gradient-to-b from-white via-[#F5F9FF] to-white`, `py-20 lg:py-24`.
  - **`LandingProofStrip.tsx`** (NOUVEAU, remplace `LandingProof.tsx` supprimé) : bande blanche `mt-10`-style `grid gap-4 rounded-[28px] border border-slate-200 bg-white p-5 shadow-xl shadow-blue-950/5 sm:grid-cols-2 lg:grid-cols-5` avec **5 blocs** (Création d'entreprise / Documents commerciaux / Suivi ventes-achats-stock / Comptabilité structurée / Données centralisées), icône `h-11 w-11 rounded-xl bg-blue-50 text-[#0B63F6]`, titre navy, détail slate.
  - **`LandingModules.tsx`** : réécrit en "Modules clés" — eyebrow `MODULES CLÉS` bleu, titre « Tous les modules essentiels pour gérer votre PME », sous-titre spec, **6 cards propres** (Ventes / Achats / Stock / Trésorerie / Comptabilité / Documents, descriptions exactes du spec, icônes bleues `bg-blue-50`), `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6`, badges plans retirés, fond section `#F8FBFF`.
  - **Palette harmonisée** : `#F4F8FF` → `#F8FBFF` (Why/Features/Morocco/Pricing/Comparison/Resources/Contact/Workflow), hero `#F5F9FF` — direction unique blanc/bleu/navy (`#061B3F` / `#0B63F6` / `#38A3FF` / bordure `#E2E8F0`).
  - Aucun `min-w-[1200px]`/`w-screen`/`max-w-none`/`h-[900px]`/`object-cover` dans le landing (grep zéro). Vérifié : lint 0 erreur, build 113 routes OK, HTML prérendu + serveur live (grid 0.85fr/1.15fr, logo width=165, nav `-translate-x-1/2`, `bg-[#0B63F6]`, gradient h1, proof 5 items, modules 6 cards, `overflow-x-clip`, zéro blob/cube/floating/mockup), `/login?mode=register` 200. **PENDING**: deploy.
- **Hero landing : mockup HTML → vraie image ERP (next/image)** — le mockup 100% CSS du hero (`LandingHero.tsx` : barre de fenêtre, sidebar fake, 3 KPI cards, chart 20 barres, lignes tableau, 4 cartes flottantes `landing-float`) est **supprimé** et remplacé par une vraie image statique `public/landing/felexia-erp-dashboard-hero.png` (1672×941, ratio 16:9, 1,15 Mo — **copiée depuis `public/brand/felexia-erp-dashboard-hero.png`**, l'original généré fourni par l'utilisateur ; aucune image externe ni asset BJIT). Conteneur premium : `relative mx-auto mt-14 w-full max-w-6xl` + halo `absolute inset-8 rounded-[48px] bg-blue-500/10 blur-3xl -z-10` + cadre `overflow-hidden rounded-2xl md:rounded-[36px] border border-slate-200 bg-white shadow-[0_30px_100px_rgba(15,23,42,0.12)] ring-1 ring-slate-900/5`. `<Image>` : `src="/landing/felexia-erp-dashboard-hero.png"`, `alt="Aperçu du tableau de bord FelexiaERP"`, `width={1672} height={941}`, `priority` (preload), `sizes="(max-width: 768px) 100vw, (max-width: 1200px) 90vw, 1150px"`, `className="h-auto w-full object-contain"` (aucune largeur fixe, pas de `w-screen`/`min-w-*`, pas de déformation, pas de scroll horizontal — wrapper `overflow-x-clip` + `max-w-6xl`). Éléments conservés : badge, h1, subtitle, CTA, trust points, halos bleus + cubes décoratifs discrets ; cartes flottantes et keyframes `landing-float` **retirés** de `globals.css` (plus aucune référence dans le code). WebP non généré (GDI+ .NET ne sait pas encoder WebP — laissé PNG, le build ne casse pas). Vérifié : lint 0 erreur, build 113 routes OK, HTML prérendu contient `<img ... srcSet=/_next/image?url=%2Flanding%2Ffelexia-erp-dashboard-hero.png...>` + alt + width/height + sizes + preload, zéro `app.felexia.ma`/`landing-float`/`Solde prévu` (mockup éliminé), serveur live : image directe 200 image/png (1 156 213 o) + optimiseur 200, `/` sert la nouvelle version. **PENDING**: deploy.
- **Landing refonte « Executive Clarity » (14 sections, light premium SaaS)** — skill `ui-ux-pro-max` demandé par l'utilisateur **indisponible** dans l'environnement (seul `customize-opencode` existe) → méthode appliquée manuellement (audit → direction → implémentation → QA), signalé explicitement. Nouvelle composition `src/app/page.tsx` + metadata SEO/OG fr_MA : `LandingHeader` (existant, conforme spec) + `LandingHero` (réécrit : h1 `text-4xl sm:text-6xl lg:text-7xl` navy `#061B3F`, accent gradient `from-[#38A3FF] to-[#1E66D0]`, badge pill, CTA primaire `from-[#1E66D0] to-[#155EEF]` vers `/login?mode=register` + secondaire `#pricing`, 3 trust points, mockup CSS premium — barre de fenêtre, sidebar app, 3 KPI cards, chart prévisions 20 barres, lignes tableau, 4 floating cards `landing-float`, cubes décoratifs bleus) + `LandingProof` (NOUVEAU : bande 4 items blanc/border) + `LandingModules` (réécrit : 9 cards avec badges Essentiel/Business/Premium, id="modules") + `LandingWorkflow` (NOUVEAU : 2 timelines chips + flèches — flux vente Client→Devis→Commande→Livraison→Facture→Paiement→Comptabilité, flux achat Fournisseur→Commande→Réception→Facture→Paiement→Trésorerie→Comptabilité, dernière étape = chip gradient brand) + `LandingWhy` (réécrit : 6 arguments spec — Maroc/TVA+MAD, PME & équipes, connecté, pilotage temps réel, cloud, évolutif — + CTA, id="why") + `LandingFeatures` (réécrit : 8 familles à onglets avec badges de plan par famille et par feature, id="features", `"use client"` ajouté) + `LandingMorocco` (NOUVEAU : 4 cards conformité marocaine + bandeau ambre « fonctions préparatoires », fond `#F4F8FF`) + `LandingPricing` (réécrit : données **`SUBSCRIPTION_PLANS`** — Essentiel 290/2 900, Business 690/6 900 recommandé, Premium 1 290/12 900 MAD — toggle mensuel/annuel par défaut annuel, 3 cards même hauteur, Business mis en avant (badge « Recommandé », ring bleu, CTA gradient), `"use client"` ajouté, id="pricing") + `LandingComparison` (NOUVEAU : tableau Excel vs FelexiaERP, header bandeau gradient) + `LandingResources` (réécrit : 3 articles spec + badge « Bientôt disponible », id="resources") + `LandingContact` (titre spec « Vous voulez voir FelexiaERP en action ? », formulaire inchangé TODO Server Action, id="contact") + `LandingFooter` (© FelexiaERP, mention préparatoire conservée). **`LandingCta.tsx` supprimé** (intégré à la nouvelle structure) — zéro référence restante. Palette strictement spec : light seulement, navy `#061B3F`/`#0B2A5B`, primary `#1E66D0`, azure `#38A3FF`, pas de violet sur le brand (violet restreint aux badges Premium), fonds `#F4F8FF` alternés. Vérifié : lint 0 erreurs, build 113 routes OK, HTML prérendu `/` — h1, 14 sections, ancres id=modules/why/features/pricing/resources/contact, zéro `#pourquoi`, scroll-mt-24 ×9, `mode=register` ×n, prix MAD présents (espaces insécables étroites U+202F via `toLocaleString("fr-FR")`), logo width="150"/"180", `overflow-x-clip`. **PENDING**: deploy.
- **`/` redirect to /login fixed — landing is now always public** — root cause: `src/lib/supabase/middleware.ts` (wired via `src/proxy.ts` proxy, Next 16 middleware) had a dedicated `/` block: logged-in → redirect `/dashboard`, else → **always redirect `/login`** (including when `getUser()` threw). Fix: `/` now returns `supabaseResponse` immediately (public pass-through) before any auth check — no redirect to /login or /dashboard for anyone, anonymous or logged-in. `src/app/page.tsx` was already a pure public landing (no auth calls). All other middleware behavior untouched: `/login` not in matcher (always public), every other matched route (`/dashboard`, `/vente`, `/achats`, `/facturation`, `/stock`, `/tresorerie`, `/comptabilite`, `/parametres`, `/onboarding`, etc.) still 307 → `/login?next=<path>` for anonymous users. Verified live with `npm start` + curl: `/` → 200 landing HTML (no redirect), `/login` → 200, `/dashboard` → 307 `/login?next=%2Fdashboard`, `/vente` → 307 `/login?next=%2Fvente`, `/brand/felexiaerp-logo.png` → 200 image/png. Lint 0 errors, build 113 routes OK. NOTE: middleware cookie refresh no longer happens on `/` (accepted — landing has no session-dependent UI).
- **Landing logo overflow / horizontal scroll FIXED (controlled sizes, no more w-auto)** — user reported the header logo rendering gigantic (cube+text escaping the header, menu compressed, "Démarrer gratuitement" offscreen, horizontal scroll). Root cause: `BrandLogo` horizontal rendered `h-auto max-w-full` with NO explicit CSS width, and the header passed `className="w-40 lg:w-auto"` — at `lg+`, `width:auto` lets the browser fall back to the **native PNG size (867×288)**, blowing up the logo and pushing everything off-screen. Fixes (deterministic, per spec):
  - `BrandLogo.tsx` rewritten with strict fixed sizes (never native size): `HORIZONTAL_SIZES` = sm `w-[150px] max-h-[42px]` (attrs 150×50), md `w-[180px] max-h-[60px]` (180×60), lg `w-[220px] max-h-[64px]`, xl `w-[260px] max-h-[74px]` — classes + numeric width/height attrs + `block h-auto max-w-full shrink-0 object-contain`; `height` override now renders with inline `style={{width,height}}` (deterministic, no w-auto anywhere). Effective rendered sizes: sm 126×42, md 180×60, lg 192×64, xl 222×74. Mark sizes per spec: sm 32/md 40/lg 52/xl 64. NOTE: md max-h raised 50→60 (spec) so desktop header logo hits the acceptance range ~170-190px wide; documented.
  - `LandingHeader.tsx`: removed the dangerous `className="w-40 lg:w-auto"`; now TWO instances — mobile `<BrandLogo size="sm" priority>` (hidden on lg+), desktop `<BrandLogo size="md" priority>` (hidden below lg) — 126×42 in h-16 mobile / 180×60 in h-20 desktop; actions row `shrink-0`.
  - `page.tsx` landing wrapper: `overflow-x-clip` added (protection; real source fixed at the logo).
  - Audited all landing components for other overflow sources: hero blobs/floating cards are inside `relative overflow-hidden` section, CTA band blobs inside `overflow-hidden rounded-3xl`, all sections max-w-7xl — none overflow.
  - Verified: lint 0 errors, build 113 routes OK, prerendered `/` HTML contains both logo sizes (width="150"/"180"), `max-h-[42px]`, zero `w-auto`, `overflow-x-clip`, `lg:h-20`, `shrink-0` actions, xl nav.
- **Landing header fully redesigned (premium, brand-aligned) + hero/sections harmonized** — root cause of the "cramped/raw" look: `BrandLogo` `size="md"` = 190px wide = **63px tall natural height inside a `h-16` (64px) header** — the logo filled 100% of the header height (zero breathing room, "colle"), buttons were small uneven `py-2 rounded-xl` blocks, and logo+nav+3 buttons totaled ~1330px which **overflows at the lg breakpoint (1024px)**. Fixes:
  - `BrandLogo.tsx` sizes at the time: sm=**170**/md=**210**/lg=**260**/xl=300 (superseded by the controlled-size rewrite above — see newest entry);
  - `LandingHeader.tsx` rewritten: sticky `top-0 z-50 border-b border-slate-200/70 bg-white/85 backdrop-blur-xl`; container `mx-auto flex h-16 max-w-7xl items-center justify-between gap-4 px-4 sm:px-6 lg:h-20 lg:px-8`; logo two-instance responsive (see newest entry; the initial `w-40 lg:w-auto` variant was the overflow bug later fixed); nav `hidden xl:flex items-center gap-6` (6 links, accented labels "Pourquoi Felexia"/"Fonctionnalités"/"Ressources", `text-sm font-medium text-slate-600 hover:text-[#0B2A5B]`, whitespace-nowrap); actions `hidden lg:flex items-center gap-3` (Connexion ghost + Démarrer gradient always, "Demander une démo" `hidden xl:inline-flex`) — full desktop layout only at **xl (1280+)** because 6 links + 3 buttons + 170px logo ≈ 1200px cannot fit at lg (spec's gap-8 would overflow; gap-6 chosen, documented in code); hamburger `h-11 w-11 rounded-2xl xl:hidden`; mobile menu = white/95 blur dropdown with nav links + Connexion + Démarrer gratuitement (h-11 full-width). **Breakpoints: <lg logo+hamburger; lg–xl logo + 2 CTAs + hamburger; xl+ full nav+actions.**
  - Buttons per spec: demo = `h-11 rounded-2xl border border-slate-200 bg-white px-5 text-sm font-semibold text-slate-700 shadow-sm hover:border-blue-200 hover:bg-blue-50 hover:text-blue-700`; Connexion = ghost `h-11 rounded-2xl px-4 ... hover:bg-slate-100`; Démarrer = **brand gradient** `h-11 rounded-2xl bg-gradient-to-r from-[#1E66D0] to-[#155EEF] px-6 text-white shadow-lg shadow-blue-600/25`.
  - `LandingHero.tsx` harmonized: pt `pt-12 sm:pt-20 lg:pt-24` (under 80px sticky header), bg `via-[#f4f8ff]`, violet blobs → sky/blue, decorative translucent cube-inspired squares (border-4 `#3B9BEF`/`#155EEF`), h1 `text-[#0B2A5B]` navy + accent `from-[#3B9BEF] to-[#155EEF]`, CTAs h-12 matching header language, mockup violet accents → brand blues.
  - Anchors per spec: ids renamed `#pourquoi`→`#why`, `#fonctionnalites`→`#features`, `#ressources`→`#resources` (zero remaining refs to old ids); `scroll-mt-24` added to all 6 anchored sections (modules/why/features/pricing/resources/contact).
  - Verified: lint 0 errors, build 113 routes OK, prerendered `/` HTML contains sticky header, 170px logo, `xl:flex` nav, accented CTA "Demander une démo", brand gradient, navy h1, scroll-mt-24 ×9, no violet in hero. NOTE: user attached fel.png screenshot I could not read (no image support) — design validated against code/spec only.
- **Landing "Demarrer gratuitement" CTAs now point to the real registration route** — `/register` does NOT exist as a standalone page; the actual account-creation form is `RegisterAdminForm` rendered at `/login?mode=register` (after signup → `/onboarding/entreprise`). The old target `/onboarding/inscription` requires a session (page redirects to `/login` for anonymous users) and the middleware bounces anonymous visitors to the **login** form, not registration. All 6 CTAs updated from `/onboarding/inscription` → `/login?mode=register`: `LandingHero.tsx` (primary CTA), `LandingHeader.tsx` (desktop + mobile), `LandingCta.tsx`, `LandingPricing.tsx` (`ctaFor.essentiel`), `LandingFooter.tsx` (`produitLinks`). "Se connecter" → `/login`, "Demander une demo"/"Parler a un conseiller" → `#contact`, "Voir les tarifs" → `/pricing`/`#pricing` unchanged. Verified: prerendered `/` HTML contains `mode=register` links, zero `/onboarding/inscription` refs, `/login?mode=register` → 200, lint + build 113 routes OK.
- **FelexiaERP logo rendering fixed (too-small issue) + transparent logo re-uploaded** — user re-uploaded the official logo (`feelxianewlogo.png`, now 867×288, Format32bppArgb = **transparent background**, 92 KB) and complained the logo rendered too small in login/landing/onboarding/sidebar. Root cause: `BrandLogo` sized by **height** (sm 24 / md 40 / lg 56 → real widths ~72–169px) and call sites passed tiny numeric heights (`height={26–40}`) plus clamping classes (`max-h-9 max-w-[190px]` in sidebar). Fix:
  - **Assets regenerated from the new transparent source**: `public/brand/felexiaerp-logo.png` + **new `felexiaerp-logo-horizontal.png`** (canonical horizontal asset, `BRAND.logoHorizontal` now points to it), `felexiaerp-mark.png` (512²), `-mark-32/48.png`, `felexiaerp-apple-180.png`, `public/favicon.ico` — all re-created via System.Drawing (script in temp), ratio now 867/288.
  - **`BrandLogo.tsx` rewritten with width-based professional sizes**: horizontal sm=**150px**/md=**190px**/lg=**240px**/xl=**300px** wide (heights derived from native ratio, object-contain, `shrink-0`, `max-w-full` for mobile, never square-cropped); `size: "sm"|"md"|"lg"|"xl"` + `height` numeric override kept; mark variant (sm 28/md 40/lg 64/xl 96) reserved for favicon/compact use only. `priority` prop maps to Next 16 `preload` + `loading="eager"` (Next 16 deprecates `priority` on next/image — verified in `node_modules/next/dist/docs/.../image.md`).
  - **Sites updated to named sizes (no more tiny numeric heights)**: `/login` top → `size="lg"` (240px, `mb-10` ≈ 40px before title) + testimonial panel → `size="xl"` (300px); landing header/footer → `md` (190px, in the 170–210px recommendation); sidebar → `size="sm"` (150px) inside a discreet white chip (`rounded-xl bg-white/95 px-3 py-2 ring-1 ring-black/5` — readable in dark mode); onboarding entreprise/societe/paiement + `/bienvenue` + `/invitation` + `/pricing` → `size="md"` in the same white chip (token-themed pages can be dark); dashboard cockpit → `size="sm"`. Logo wrapper `Logo.tsx` kept only as compat (no remaining usages).
  - Verified: lint 0 errors, `npm run build` 113 routes OK, prerendered `/` HTML contains `width="190"` logo + `felexiaerp-logo-horizontal.png` + favicon/OG links, zero refs to old `felexiaerp-logo.png` direct asset path. NOTE: mark/favicon variants remain full-logo-fitted-in-square (cube-only crop impossible without vision on the image).
- **Official FelexiaERP logo rolled out everywhere (identity swap)** — new logo `feelxianewlogo.png` (1783×592, 32bpp ARGB) installed as the official platform identity; **client-uploaded org logos on commercial documents untouched** (`print-organization-logo.tsx` still renders `identity.logoUrl` first, FelexiaERP only as fallback when an org has no logo):
  - **Assets `public/brand/`**: `felexiaerp-logo.png` (original horizontal copy), `felexiaerp-mark.png` (512×512 square, contain, transparent), `felexiaerp-mark-32.png` / `felexiaerp-mark-48.png` (favicon sizes), `felexiaerp-apple-180.png` (apple-touch-icon) — all generated from the source via System.Drawing (script in temp); `public/favicon.ico` created from the 32px mark. Old `public/branding/felexia-icon.svg` + `felexia-logo.svg` **deleted** (zero references remained).
  - **`src/lib/brand.ts`**: new `BRAND` config (name "FelexiaERP", shortName "Felexia", tagline "Mini ERP moderne pour PME marocaines", `logoHorizontal`, `logoMark`, `favicon`, icon sizes, appleTouchIcon) + `APP_BRAND` kept as alias (points to new assets) so `print-organization-logo.tsx` and legacy imports keep working.
  - **`src/components/brand/BrandLogo.tsx`** (new canonical component): props `variant: "horizontal"|"mark"`, `size: "sm"|"md"|"lg"` (24/40/56 px horizontal, 28/40/64 px mark), `showText` (mark + wordmark span), `className`, `priority` (→ loading eager), `height` (numeric override); uses next/image `object-contain`, aspect-correct dimensions (1783/592), `unoptimized`. **`Logo.tsx`** is now a compatibility wrapper (numeric `size` + `withText` → BrandLogo, re-exports BrandLogo).
  - **Replaced in**: landing header/footer (`BrandLogo horizontal md`, footer text "FelexiaERP — Mini ERP moderne pour PME marocaines."), landing hero mockup brand, sidebar header (old "F" box + Felexia/Mini-ERP text → horizontal logo h36, max-w 190px), `/login` (top horizontal md + right panel horizontal lg 56px, old blue 80px box removed), onboarding entreprise/societe/paiement + `/bienvenue` + `/invitation` + `/pricing` (horizontal logo, "Felexia" wordmarks removed), dashboard cockpit (Logo wrapper, h26).
  - **Metadata**: `layout.tsx` — title "FelexiaERP — Mini ERP moderne pour PME au Maroc", description "Gérez ventes, achats, stock, trésorerie, comptabilité et documents avec FelexiaERP.", `metadataBase` from `NEXT_PUBLIC_APP_URL`, icons: icon (mark 32/48 PNG + favicon), shortcut, apple 180; OpenGraph website fr_MA + OG image `logoHorizontal`. Landing `page.tsx` metadata updated to the same title/description.
  - **Emails**: no email templates exist (no `src/emails`, no resend lib) — nothing to change (per instructions, don't invent).
  - Verified: lint 0 errors, `npm run build` 113 routes, prerendered `/` HTML contains new assets + favicon links + new title, zero `branding/felexia*` refs. NOTE: mark/favicon variants = full logo fitted in square canvas (cube-only crop not possible without visual inspection of the source); user can drop a square mark into `public/brand/felexiaerp-mark.png` later for a tighter favicon.
- **Public landing redesigned (always-light SaaS, inspired by minierp.bjitgroup.com UX)** — old dark landing replaced by a light marketing page composed from `src/components/landing/*` (all new): `LandingHeader` (sticky blur, nav anchors #modules/#pourquoi/#fonctionnalites/#pricing/#ressources/#contact, hamburger mobile, actions → #contact, /login, /login?mode=register (registration; see the CTA entry below — superseded /onboarding/inscription for the "Demarrer gratuitement" buttons)), `LandingHero` (badge pill, gradient headline, CTAs, dashboard mockup 100% CSS with window bar + sidebar + KPI cards + bar chart + table skeletons, 4 floating cards animated via new `landing-float` keyframes in globals.css, `prefers-reduced-motion` disabled), `LandingModules` (8 module cards with lucide icons, id="modules"), `LandingWhy` (6 cards, gradient icon tiles), `LandingFeatures` (8 families Vente/Achats/Stock/Tresorerie/Comptabilite/Documents/TVA/RH, client-side selector), `LandingPricing` (**prices loaded from `SUBSCRIPTION_PLANS` in `plans.ts`**, not hardcoded: Essentiel 290/2 900, Business 690/6 900, Premium 1 290/12 900 MAD; monthly/yearly toggle, Business highlighted "Recommande", CTAs per plan), `LandingCta` (gradient band), `LandingResources` (3 static articles, badge "Bientot"), `LandingContact` (form Nom/Entreprise/Email/Telephone/Message — **client-side simulated submit with TODO**, no invented email/phone, success state), `LandingFooter` (Produit/Modules/Legal columns + preparatoire disclaimer). Metadata in `page.tsx`: title "FelexiaERP — Mini ERP moderne pour PME au Maroc", SEO description, OpenGraph website fr_MA. **Theme isolation**: globals.css `.landing` scope block re-pins the light palette (`bg-white`/`bg-slate-*`/`text-slate-*`/`border-slate-*`/`shadow-*` → light hex values) with higher-specificity `!important` overrides of the adaptive compatibility layer — landing is deliberately always light, like print views. `shadow-*` restored via `var(--tw-shadow-colored)` so `shadow-blue-600/25` tints still work. Lint + `npm run build` (113 routes, `/` static prerender) pass; prerendered HTML verified (all sections, prices, no BJIT/MiniERP/Odoo text). **PENDING**: deploy. TODO if requested: wire LandingContact to a real Server Action/table.
- **Treasury cash-flow forecast module (`/tresorerie/previsions`) + "Relevés bancaires" hidden** — Full forecast tool built on open invoices + manual items:
  - **Hide releves**: `SidebarItem.hidden?: boolean` added to `sidebar-data.ts` type + filter in `sidebar.tsx` items map; "Relevés bancaires" marked `hidden: true`; "Importer releve bancaire" removed from Command Center (`quick-create-menu.tsx`) and replaced by "Nouvelle prevision de tresorerie" (`/tresorerie/previsions/nouveau`); bank-statement entries removed from `global-search-registry.ts`; `/tresorerie/releves` page now shows a clean "Relevés bancaires bientot disponibles" placeholder (routes/components/tables untouched, all reusable).
  - **Migration `090_treasury_forecast_items.sql`**: `treasury_forecast_items` (id, organization_id, item_type, direction, source_type/id, label, description, forecast_date, amount, probability, weighted_amount, status, category, treasury_account_id, is_manual, notes, created_by, timestamps, archived_at; CHECKs on direction/item_type/status/amount/probability; indexes org/date/direction/status/source/archived; RLS = select all active members, insert/update owner/admin/accountant, **no delete policy** — archive via `archived_at`). **PENDING**: apply to DB via SQL editor.
  - **Service `src/lib/treasury/treasury-forecast.ts`**: `getTreasuryForecast(supabase, organizationId, {from, to, scenario, includeOverdue, includeManualItems, treasuryAccountId, direction})` — opening balance = sum of active `treasury_accounts.current_balance` (per-account filter supported); inflows = `customer_invoices` status in validated/sent/partially_paid/overdue, remaining_amount > 0, due_date fallback = invoice_date+30j (counted as "facture sans echeance"); outflows = `supplier_invoices` status in validated/partially_paid; probabilities: customer overdue 70%, else 100%, supplier 100%, manual = stored; scenarios: prudent (inflows ×0.7, outflows ×1), realistic (probabilities), optimistic (inflows ×1, outflows × probability); weighted = amount × prob × scenario factor; daily series from→to with opening/in/out/net/closing + isCritical (closing<0), lowestForecastBalance, criticalDaysCount; overdue items pinned to `from` day; alerts: negative-balance day (danger), 7-day outflows ≥30% of opening (warning), overdue inflows (warning), ≥5 overdue invoices (warning), overdue supplier invoices (info), net negative (warning), no active account (warning), missing due dates (info). Missing-table degradation: if `treasury_forecast_items` doesn't exist (migration pending), manual items skipped with warn, rest works. Types `TreasuryForecastItem/TreasuryForecastDaily/TreasuryForecastAlert/TreasuryForecastResult`, `FORECAST_CATEGORIES` (inflow/outflow label lists), `computeWeightedAmount` exported for actions.
  - **Actions `src/lib/treasury-forecast-actions.ts`** ("use server", org-scoped via `requireActiveWorkspace`): create/update/setStatus(planned/confirmed/realized/cancelled/ignored)/archive (soft, sets archived_at+ignored). NOTE: only async fns may be exported from "use server" files — constants like FORECAST_CATEGORIES live in treasury-forecast.ts.
  - **Page `/tresorerie/previsions`** (server component, URL-driven filters): header actions (Exporter CSV → `/tresorerie/previsions/export?from&to&scenario&overdue&account&type`, Imprimer via window.print, Actualiser, Nouvelle prevision manuelle); `TreasuryForecastFilters` (periode 7/30/60/90/custom with from/to, scenario prudent/realiste/optimiste, compte, type inflow/outflow/all, retards inclus/exclus — URL params `periode/scenario/account/type/overdue/from/to`); 6 KPI cards (solde actuel, encaissements attendus, decaissements attendus, solde previsionnel fin periode, point bas, jours critiques); alertes section; custom SVG chart `TreasuryForecastChart` (balance line + green inflow/red outflow bars + zero line + critical dots); calendrier des flux table (date, type, tiers, document, libellé with link, entrée/sortie pondérées, proba, montant pondéré, solde après flux, statut badge); encaissements/décaissements lists; prévisions manuelles table with `TreasuryForecastManualItem` actions (edit /marquer réalisée /annuler /archiver, client actions + router.refresh); hypothèses de calcul cards; EmptyState with 3 CTAs; KPI/chart/links use `--var` theme tokens. Create page `/tresorerie/previsions/nouveau` + edit `/tresorerie/previsions/[id]/edit` share `TreasuryForecastForm` (direction toggle switches category options, account combobox, probability, status select on edit).
  - **CSV export route** `/tresorerie/previsions/export/route.ts`: BOM + `text/csv; charset=utf-8`, filename `previsions-tresorerie-YYYY-MM-DD.csv`, columns Date/Direction/Source/Tiers/Document/Libellé/Montant/Probabilité/Montant pondéré/Statut/Solde prévisionnel.
  - Verified: lint clean, `npm run build` 113 routes OK (new routes present), open-invoice queries tested against DB (customer: FAC-2605-00001..00006 statuses validated/sent with remaining_amount>0; supplier: FF-2605/FF-DEMO rows OK). **PENDING**: apply migration 090; deploy.
- **Comptabilité moved into Essentiel plan** — `accounting` added to `ESSENTIEL_MODULE_KEYS` in `src/lib/subscriptions/plan-modules.ts` (now = dashboard,crm,quotes,invoicing,documents,products,purchases,stock,treasury,**accounting**,users,settings). `BUSINESS_MODULE_KEYS = [...ESSENTIEL_MODULE_KEYS]` (Business no longer adds a module on top; Premium = Business + `rh`). Sidebar Comptabilité section (`moduleKey: "accounting"`, line ~187 in `sidebar-data.ts`) auto-appears once enabled. `/comptabilite` routes (incl. TVA subpages) were previously "Module disponible dans Business" for Essentiel via `PLAN_REQUIRED_FOR_MODULE.accounting`; removed that entry in `module-upgrade-page.tsx` (kept purchases/stock/treasury/rh). `plans.ts` updated: `accounting_basic` + `journals` added to ESSENTIEL_FEATURES, Essentiel description/highlights now advertise Comptabilite (highlight reordered so it fits the first 8 shown by `featureHighlights.slice(0, 8)` in subscription-manage/plan-choice), limitHighlights now "Sans TVA avancee ni comptabilite avancee". `vat` stays Business/Premium-only as a feature. Pricing page copy "pre-comptabilite"→"comptabilite". **APPLIED to DB**: `sync:essentiel-modules` with `APPLY_SYNC=true` — 4 Essentiel orgs (TITI YA TITI, ANOUAR, ELECTRO TARI, CASADD); ELECTRO TARI + CASADD got `accounting` enabled; verified `organization_modules` has accounting enabled for all orgs. Lint + `npm run build` (113 routes) pass. **PENDING**: deploy.
- **Company logo not showing in printable documents (print/PDF) — FIXED (Next 16 SSRF local-IP block)** — Root cause was NOT the upload or identity chain: `organizations.logo_url`/`logo_path` + `company_settings` are persisted correctly, storage URL returns 200 (image/png), and `getOrganizationDocumentIdentity`→`getOrganizationLegalDisplay`→`PrintOrganizationLogo` wiring is correct. The `next/image` optimizer returned **400 "hostname resolved to private IP"** for `https://<ref>.supabase.co/storage/v1/object/public/organization-logos/**` because the Supabase storage hostname resolves to NAT64-IPv6 (`64:ff9b::ac40:95f6`, `64:ff9b::6812:260a`) which Next 16's new SSRF guard flags as private → all print views (`SalesQuotePrintView`, `CustomerInvoicePrintView`, supplier invoice/order/receipt) fell back to the Felexia logo. Fix (documented in `node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md` "Local IP Restriction (Breaking change)"): set `images.dangerouslyAllowLocalIP = true` in `next.config.mjs`. Verified live: optimizer URL returns 200/webp with the config (before: 400 private-IP, after: 200). Also replaced mocked `priority` prop with `loading="eager"` in `print-organization-logo.tsx` (Next 16 deprecates `priority` → `preload`). remotePatterns already scoped to `**.supabase.co/storage/v1/object/public/organization-logos/**` + `dangerouslyAllowLocalIP` only affects that bucket. Lint / tsc pass. NOTE: with 1 MB+ logos the optimizer also rejects non-standard widths (`w=80` → "w parameter (width) of 80 is not allowed"); `next/image` auto-uses allowed `imageSizes` so component is unaffected. **PENDING**: restart prod/build to pick up `next.config.mjs`.
- **Company logo upload fixed (1 MB Server Action limit)** — `updateCompanyAction` received the raw logo file in the action FormData; Next.js Server Actions cap at 1 MB so any logo >1 MB threw "Body exceeded 1 MB limit" (the form allowed 5 MB). Fix: `company-settings-form.tsx` now uploads the file client-side to Supabase Storage bucket `organization-logos` (`createClient` browser client, path `organizations/{orgId}/logo-{ts}-{uuid}.{ext}`), deletes the file from the FormData and sends only `logo_path`/`logo_url` to the action. `updateCompanyAction` validates the path prefix (`organizations/{orgId}/`) + extension, persists via `set_organization_logo` RPC (role-checked, updates `organizations` + `company_settings`), and deletes the previous logo. Small-file legacy fallback (action-side upload) kept. Button shows "Upload du logo..."; copy no longer recommends <1 MB. Lint / tsc / build pass.
- **Essentiel plan widened to match Command Center** — `ESSENTIEL_MODULE_KEYS` in `src/lib/subscriptions/plan-modules.ts` now = `dashboard,crm,quotes,invoicing,documents,products,purchases,stock,treasury,users,settings` (was only crm/quotes/invoicing/documents/products). Business = Essentiel + `accounting`; Premium = Business + `rh`. Effect: `/achats` (purchases), `/stock`, `/tresorerie` (treasury) are no longer "Module disponible dans Business" for Essentiel. Added `normalizeModuleKey` alias map in `plan-access.ts` (`tiers`→crm, `vente`→quotes, `facturation*`→invoicing, `articles`→products, `achats`→purchases, `tresorerie`/`treasury`→treasury, `inventory`→stock, `comptabilite`→accounting, `utilisateurs`→users, `parametres`→settings, `guide_demarrage`→dashboard) and `canAccessModule` normalizes before checking. `plans.ts` ESSENTIEL_FEATURES/featureHighlights/limitHighlights updated (Achats/Stock/Trésorerie inclus; Business advertises Comptabilité/TVA/Rôles). `activatePlanAction` now calls `upsertModulesForPlan`. New `scripts/sync-essentiel-modules.ts` (+ `npm run sync:essentiel-modules`, env `APPLY_SYNC=true` to apply, `ORG_NAME` filter, `STRICT_MODULES=true` to disable non-Essentiel modules) upserts corrected modules into `organization_modules` for existing Essentiel orgs (column is `enabled`, NOT `is_enabled`). All creation paths already funnel through `ensureDefaultTrialAndModulesNoRevalidate`→`upsertModulesForPlan`, so new orgs get the corrected modules automatically. Command Center routes verified to exist (`/stock/entrees/new`, `/stock/ajustements/new`, `/stock/emplacements/new`, `/tresorerie/comptes/new`, `/tresorerie/mouvements/new`, `/tresorerie/releves/import`, all `/achats/*/new`). Lint / tsc / `npm run build` all pass. **APPLIED to DB**: migration 089 (`products` added to `modules_catalog`) + `sync:essentiel-modules` run for TITI YA TITI, ANOUAR, CASADD (CASADD verified: purchases/stock/treasury/products all enabled). **PENDING**: SQL control queries.
- **Theme refactor → light-first premium (default light, toggle dark)** — CSS semantic tokens in `src/app/globals.css` (`:root` light: bg `#F4F6FA`, card white, primary `#1D4ED8`, accent gold `#B08A3E`; `.dark` slate-blue; radii 8/12/16; `@theme inline` exposes `--color-*` incl. sidebar tokens; no global `color-scheme: dark`). **Compatibility layer** remaps legacy `bg-white`/`text-slate-*`/`border-*`/`shadow-*` classes to tokens so non-refactored screens stay readable in both themes. `next-themes` installed: `ThemeProvider` (`src/components/theme/theme-provider.tsx`, `defaultTheme="light"`, `storageKey="felexia-theme"`, `enableSystem={false}`) wraps body in `src/app/layout.tsx`; `ThemeToggle` (`theme-toggle.tsx`, hydration-safe via `useSyncExternalStore` — lint rule `react-hooks/set-state-in-effect` forbids mounted useState) in topbar, visible on mobile. Refactored to tokens: topbar, sidebar (active item = card bg + ring + primary left bar), ui primitives (button/badge/card/table/input/select/textarea/kpi-card), global-search/quick-create-menu/notification-dropdown popovers, dashboard page + chart, pricing page, onboarding (CompanyLookupStep/CompanyConfirmationForm/entreprise), document-flow-map, HR screens (employee form, contract form, employee detail, section page, dashboard, employees page, paie), erp page-header + module-upgrade-page, trial-started-banner, plan-choice-client, parametres/profil, documents page buttons. `bg-white`→`var(--card)`, gold-gradient CTAs→`var(--primary)`, `text-white`→`var(--primary-foreground)` on buttons. Deliberately kept dark: landing `app/page.tsx` hero, `mobile-app-banner`, print views (`#2d2490`), modal scrims. Lint / tsc / `npm run build` (113 routes) all pass.
- **Default subscription refactor → Essentiel** — default plan is now `essentiel` (was Business). New `src/lib/subscriptions/plans-config.ts` (`DEFAULT_PLAN_KEY`) + `plan-modules.ts` (ESSENTIEL/BUSINESS/PREMIUM_MODULE_KEYS, `getModulesForPlan`, `upsertModulesForPlan`). `plans.ts` `PlanCode=PlanKey`, Essentiel=crm/quotes/invoicing/documents/products, Premium=Business+rh. `plan-access.ts` rewritten (`getOrganizationSubscription` joins plan, insert-only `upsertTrialForPlan` never overwrites paying orgs, `ensureDefaultTrialAndModulesNoRevalidate` / `startDefaultTrialAndEnableModules`, Business helpers are explicit-upgrade-only). Canonical `requireActiveSubscription` in `src/lib/auth/require-subscription.ts` (Essentiel fallback); `subscription-guard.tsx` re-exports it. `saas.ts` cleaned (`requireValidSubscription` grants Essentiel, `canAccessModule` pure read, `startDefaultTrial`). Moved all Business fallback out of register/create/start-trial actions, `/api/subscriptions/start-trial`, onboarding pages, banners, sidebar (Articles section = moduleKey `products`), `module-guard-layout` renders local `ModuleUpgradePage` instead of redirecting. Migration `089_add_products_module.sql` adds `products` to `modules_catalog`. French copy updated ("Essai Essentiel") across register forms, onboarding (entreprise/modules/paiement), bienvenue, dashboard banner, subscription-manage, `pricing`, landing `app/page.tsx`, `loading-overlay`, `resolve-onboarding-redirect` (no more global `/parametres/abonnement` redirect). Scripts updated: `seed-demo-societe-demo.ts` supports `DEMO_PLAN` env (default business, essentiel skips HR seed), `reset-business-trial-to-launch-offer.ts` supports `RESET_PLAN` (default essentiel), `enable-rh-module-for-business-orgs.ts` uses PREMIUM_MODULE_KEYS. Lint / tsc / `npm run build` all pass. `canAccessApp` deep-import path changed to `@/lib/subscriptions/subscription-access`. **PENDING**: apply migration 089 to the DB; provide SQL control queries.
- **Audit fixes** — Next.js upgraded 16.2.6 → 16.3.0 (0 npm audit vulns), dead code removed (`cheerio`, `marocfacture.ts`, `/api/company-lookup`, `rate-limit` now wired in), console.log cleanup, 6 lint warnings fixed, `.env.example` added.
- **Invitation flow fixed** — migration 088 adds token-gated security definer RPCs `app_private.get_invitation_by_token` / `accept_invitation` (granted to `anon`); `/invitation` page and `/api/invitations/accept` now use them. RLS previously blocked anonymous token lookup, breaking every invite.
- **Company search rate limit enforced** — `/api/company/search` now calls `isRateLimited(user.id, 1, 30s)` → 429. Old `/api/company-lookup` route (which only wrote counters, never blocked) deleted.
- **Company search provider switched to synta-iq** — `src/lib/ice/providers/syntaiq.ts` uses `GET https://www.synta-iq.com/api/ma/search?q=<ice>` (REST JSON, no scraping/CSRF). Provider chain simplified to call synta-iq exclusively.
- **HR payroll period duplicate fix** — `createHrPayrollPeriodAction` checks for existing period before insert, redirects to existing if found.
- **HR embed ambiguity fix** — replaced ambiguous Supabase embeds (`department:hr_departments`, `position:hr_positions`) with separate queries.
- **Company search feature** — `CompanyLookupStep.tsx` modal popup flow, `company-search.ts` cache (SHA-256, TTL 30j/7j, no stale "not_found"), `rate-limit.ts` (1 req/30s), `/api/company/search` (Zod + rate limits).
- **RH module** — sidebar between Trésorerie and Comptabilité, migration 082, `upsertBusinessModulesForOrganization` checks `modules_catalog`.
- **TVA declarations** — migration 080/081, full CRUD, auto-fix, DGI export integration, anomaly classification badges.
- **Supplier receipts** — stock moves, invoice link/create, financial totals, archive guard, signature blocks.
- **TVA export anomalies** — actionable `correctionUrl`/`correctionLabel`/`returnTo`, validation panel.

### Key Decisions
- **FelexiaERP logo ≠ client org logo** — platform branding (`BRAND`/`BrandLogo`) is used on system pages/landing/auth/favicon only; commercial documents (devis, factures, BL, commandes, avoirs, réceptions) keep `identity.logoUrl` first in `print-organization-logo.tsx`, FelexiaERP appears only as fallback when the org has no logo. Never replace the client logo.
- **"Relevés bancaires" masqué, jamais supprimé** — `SidebarItem.hidden` (item-level, like section-level `hidden` pour CRM) ; routes `/tresorerie/releves*`, composants, tables `bank_statement_imports/lines`, actions d'import/rapprochement intactes. Page `/tresorerie/releves` = placeholder propre. Global search ne référence plus les releves (seulement la navigation visible).
- **Forecast = lecture des tables sources (jamais de duplication)** — factures clients/fournisseurs lues à la volée (`remaining_amount` pré-calculé et maintenu par `recalculateInvoicePaymentStatus` / `computeSupplierInvoicePaymentSummary`) ; `treasury_forecast_items` sert UNIQUEMENT aux lignes manuelles. Migration non appliquée → le service dégrade proprement (warn + items manuels vides), jamais de crash.
- **Scénarios = pondération sur `weighted_amount`** (prudent: entrées ×0.7/sorties ×1 ; réaliste: probabilités ; optimiste: entrées ×1/sorties ×probabilité) — factures clients échues probabilité 70%, sinon 100% ; fournisseurs 100% ; manuel = valeur saisie.
- **Essentiel now includes Comptabilité (module `accounting`)** — the whole `/comptabilite` subtree (incl. TVA pages, same guard) is Essentiel; `vat` stays a Business/Premium **feature key** for pricing display only (no separate TVA module guard). Business = Essentiel modules exactly; Premium = Business + `rh`.
- **Essentiel now includes everything the Command Center shows** (achats=purchases, stock, tresorerie=treasury) — guard pages stay but no longer trigger for those modules. Business/Premium keep accounting/rh as differentiators; `organization_modules.enabled` (NOT `is_enabled`) is the stored column.
- **CSS semantic variables are the single source of truth** for theme colors; legacy `bg-white`/`text-slate-*`/`border-*`/`shadow-*` classes are remapped to tokens in `globals.css` (compatibility layer) so un-refactored screens adapt to light AND dark automatically.
- **Light = default on load** (defaultTheme="light", enableSystem=false); dark only via header toggle, persisted in `felexia-theme` localStorage; no global `color-scheme: dark`, no hydration flash (provider + `suppressHydrationWarning` + Toggle uses `useSyncExternalStore` for mounted, NOT `useState`+`useEffect` which violates `react-hooks/set-state-in-effect`).
- Theme toggle stays visible on mobile; sidebar active item = card bg + primary left bar; gold/accent reserved for small brand touchpoints (logo, avatar), never big CTAs (CTAs = `var(--primary)`).
- **Public landing = deliberately always light** (`.landing` scope in globals.css re-pins the light palette with higher-specificity `!important` overrides of the compatibility layer; root div has class `landing` in `page.tsx`). Deliberately dark regardless of theme: `mobile-app-banner`, print views (`#2d2490`), modal scrims.
- **Unique default plan key = `essentiel`** (never `essential`/`Business`); Business/Premium only via explicit upgrade. `normalizePlanCode` maps `pro`→premium.
- **`upsertTrialForPlan` is insert-only** when no subscription exists — never overwrites an existing org's plan, protecting paying orgs.
- **No global redirect to `/parametres/abonnement`** for expired/canceled access (`resolve-onboarding-redirect` returns null); handled locally by `ModuleUpgradePage` / banners.
- **Canonical guard** = `requireActiveSubscription` (`src/lib/auth/require-subscription.ts`); `subscription-guard.tsx` is a re-export only.
- Migration 089 adds `products` to `modules_catalog`; `upsertModulesForPlan` filters out keys missing from the catalog.
- Business trial helpers (`ensureBusinessTrialAndModulesNoRevalidate`, `startBusinessTrialAndEnableModules`, `createDefaultBusinessTrial`) remain for explicit Business upgrade paths only.
- synta-iq is the single company lookup source: clean JSON, no scraping, no cheerio (dependency removed)
- `past_due` = grace period; only `canceled` blocks access
- `canAccessApp()` is single source of truth for subscription access
- Cache never returns "not_found" — always re-queries providers
- Invitation acceptance is token-gated via security definer RPCs (migration 088), never via RLS grants to anon

### Relevant Files
- `src/components/ui/button.tsx` — **composant Button unique du projet (EXISTE, ~200 usages)** : variantes tokens `primary/secondary/ghost/danger` (restaurées à l'identique) + variantes landing `landingPrimary` (fond uni `#1456B8`, `!text-white`, `hover:!text-white`, contenu `relative z-10`) et `landingSecondary` (`#DCE3EF`/`#0F2548`) ; base landing **sans tailles** (h-12/px-7 ou h-11/px-5 passés par appel — `cn` = simple join, pas de tailwind-merge) ; `asChild` via `cloneElement` (pas de slot/cva installés)
- `public/brand/` — FelexiaERP official assets: `felexiaerp-logo-horizontal.png` (canonical horizontal, 867×288 transparent — BRAND.logoHorizontal), `felexiaerp-logo.png` (same source copy), `felexiaerp-mark.png` (512²), `-mark-32/48.png`, `felexiaerp-apple-180.png`; `public/favicon.ico`. Old `public/branding/*.svg` deleted
- `src/lib/brand.ts` — `BRAND` (name FelexiaERP, logoHorizontal → `felexiaerp-logo-horizontal.png`, logoMark, favicon, icons) + `APP_BRAND` alias for legacy imports
- `src/components/brand/BrandLogo.tsx` — canonical logo component; **strict fixed sizes** (never native size): horizontal sm `w-[150px] max-h-[42px]` / md `w-[180px] max-h-[60px]` / lg `w-[220px] max-h-[64px]` / xl `w-[260px] max-h-[74px]` (attrs 150×50/180×60/220×74/260×86, object-contain, block, shrink-0); mark sm=32/md=40/lg=52/xl=64; `height` override → inline style; `preload`/`loading` via priority; `Logo.tsx` = compat wrapper (no remaining usages)
- `src/app/layout.tsx` — metadata: title "FelexiaERP — Mini ERP moderne pour PME au Maroc", description, `metadataBase` (NEXT_PUBLIC_APP_URL), icons (icon 32/48 + favicon, shortcut, apple 180), OG website fr_MA + OG image
- `src/components/shared/print-organization-logo.tsx` — **untouched**: org logo first (`identity.logoUrl`), APP_BRAND fallback now = new FelexiaERP horizontal PNG
- `src/app/page.tsx` — public landing: metadata SEO (title "FelexiaERP — Mini ERP moderne pour PME au Maroc", description, OG fr_MA) + composition `LandingHeader/Hero/Modules/Why/Features/Pricing/Cta/Resources/Contact/Footer`, root div class `landing` (always-light)
- `src/components/landing/*.tsx` — 10 landing components; `LandingPricing` reads `SUBSCRIPTION_PLANS` (real MAD prices), `LandingContact` form simulated (TODO: Server Action), `LandingHero` = CSS dashboard mockup + floating cards (`landing-float` keyframes in globals.css)
- `src/app/globals.css` — `.landing` scope block: re-pins light palette (bg/slate/text/border/shadow overrides `!important`), `landing-float` keyframes + `prefers-reduced-motion`
- `src/lib/treasury/treasury-forecast.ts` — `getTreasuryForecast` (soldes + factures clients/fournisseurs + items manuels, scénarios, série journalière, alertes), types forecast, `FORECAST_CATEGORIES`, `computeWeightedAmount`
- `src/lib/treasury-forecast-actions.ts` — create/update/setStatus/archive prévisions manuelles ("use server", org-scoped)
- `src/app/tresorerie/previsions/{page.tsx,nouveau/page.tsx,[id]/edit/page.tsx,export/route.ts}` — page complète + formulaire + CSV
- `src/components/treasury/treasury-forecast-{filters,chart,form,manual-item}.tsx` — UI prévisions
- `src/components/layout/sidebar-data.ts` + `sidebar.tsx` — `SidebarItem.hidden`, Relevés bancaires masqué ; `quick-create-menu.tsx` — nouvelle prévision à la place d'import relevé ; `global-search-registry.ts` — entrées relevés retirées
- `src/app/tresorerie/releves/page.tsx` — placeholder "bientot disponibles" (routes/actions/tables intactes)
- `supabase/migrations/090_treasury_forecast_items.sql` — table prévisions manuelles + RLS (select membres actifs, insert/update owner/admin/accountant, pas de delete) — **PENDING: à appliquer**
- `src/lib/subscriptions/plan-modules.ts` — `ESSENTIEL_MODULE_KEYS` (incl. purchases/stock/treasury/accounting), other plan keys, `getModulesForPlan`, `upsertModulesForPlan`
- `src/lib/subscriptions/plan-access.ts` — `normalizeModuleKey` alias map, `canAccessModule`, trial/module helpers
- `src/lib/subscriptions/plans.ts` — plan features/highlights for pricing + subscription-manage display
- `scripts/sync-essentiel-modules.ts` — sync `organization_modules` for existing Essentiel orgs (`APPLY_SYNC=true` to apply, `ORG_NAME`, `STRICT_MODULES`)
- `src/app/globals.css` — theme tokens (`:root` light + `.dark`), `@theme inline` mapping, compatibility layer, premium-card/luxury-border, print
- `src/components/theme/theme-provider.tsx` + `theme-toggle.tsx` — next-themes provider (light default, `felexia-theme` storage) + hydration-safe toggle
- `src/app/layout.tsx` — `<ThemeProvider>` wraps body; `src/components/layout/{topbar,sidebar}.tsx` — refondu magenta
- `src/components/ui/{button,badge,card,table,input,select,textarea,kpi-card}.tsx` — primitives en tokens
- `src/components/erp/{page-header,module-upgrade-page}.tsx`, `src/components/hr/*`, `src/app/documents/page.tsx`, `src/app/parametres/profil/page.tsx`, `src/components/subscriptions/plan-choice-client.tsx`, `src/components/dashboard/trial-started-banner.tsx` — balayage legacy dark→tokens
- `src/lib/subscriptions/plans-config.ts` — plan keys, `DEFAULT_PLAN_KEY="essentiel"`, labels, `normalizePlanKey`
- `src/lib/subscriptions/plan-modules.ts` — `ESSENTIEL/BUSINESS/PREMIUM_MODULE_KEYS`, `getModulesForPlan`, `upsertModulesForPlan`
- `src/lib/subscriptions/plan-access.ts` — `getOrganizationSubscription`, default/Business trial helpers, module upserts
- `src/lib/auth/require-subscription.ts` + `src/components/auth/subscription-guard.tsx` — canonical guard (re-export)
- `src/lib/saas.ts` — `canAccessModule`/`getUserOnboardingStatus` (Essentiel default), pure module checks
- `src/components/erp/module-upgrade-page.tsx` + `module-guard-layout.tsx` — local "module in Business/Premium" page
- `src/components/layout/sidebar-data.ts` + `src/app/articles/layout.tsx` — Articles section, guard `products`
- `supabase/migrations/089_add_products_module.sql` — adds `products` to `modules_catalog`
- `scripts/seed-demo-societe-demo.ts`, `scripts/reset-business-trial-to-launch-offer.ts` — `DEMO_PLAN`/`RESET_PLAN` env support; `scripts/enable-rh-module-for-business-orgs.ts` uses PREMIUM_MODULE_KEYS
- `src/lib/ice/providers/syntaiq.ts` — synta-iq provider (sole lookup source)
- `src/lib/ice/company-lookup-provider.ts` — simplified, delegates to synta-iq
- `src/lib/company-lookup/types.ts` — includes `"synta-iq"` source
- `src/lib/company-search.ts` — cache facade
- `src/lib/rate-limit.ts` — in-memory limiter, wired in `/api/company/search`
- `src/app/api/company/search/route.ts` — POST endpoint (Zod + auth + rate limit 1/30s)
- `src/app/invitation/page.tsx` + `src/app/api/invitations/accept/route.ts` — token-gated RPC flow
- `supabase/migrations/088_fix_invitation_anonymous_access.sql` — invitation RPCs
- `src/lib/hr/hr.ts` / `src/lib/hr/actions.ts` — HR embed + payroll fixes
- `src/components/onboarding/CompanyLookupStep.tsx` — search UI with modal confirmation
